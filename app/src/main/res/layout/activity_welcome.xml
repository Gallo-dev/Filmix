<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.presentation.welcome.WelcomeActivity"
    android:background="@color/backgroundApp">

    <ImageView
        android:id="@+id/logo_filmix"
        android:layout_width="300dp"
        android:layout_height="500dp"
        android:src="@drawable/logo_filmix"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@id/logo_filmix"
        android:layout_marginTop="80dp"
        android:text="@string/welcome"
        android:textColor="@color/grey"
        android:textSize="60sp"
        android:gravity="center"/>

    <ImageView
        android:id="@+id/ic_arrow_go"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:src="@drawable/ic_arrow"
        app:layout_constraintBottom_toBottomOf="@id/logo_filmix"/>




</androidx.constraintlayout.widget.ConstraintLayout>